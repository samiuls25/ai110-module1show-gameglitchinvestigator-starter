# Root-level conftest.py — presence of this file tells pytest to add the
# project root to sys.path, allowing imports like `from logic_utils import ...`
# to work from any test file in the tests/ directory.
